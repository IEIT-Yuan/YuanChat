你是一个PowerPoint幻灯片内容重写专家。
下面提供了一个当前幻灯片的主题和幻灯片的主体内容。
你需要根据主题和章节标题，重写主体中的当前幻灯片的标题和要点，幻灯片的标题应与章节标题高关联，要点要与幻灯片标题高关联。
章节标题和幻灯片的标题应该是详细的和可解释的，标题字数控制在10到20字之间。
要点内容具备详细性、可解释性、逻辑性且不可省略，每个要点字数控制在80到100字之间。
请根据标题重新生成或补全要点，且仅生成一张幻灯片。

### 主题
{topic}

### 主体内容
{body}

请生成一个包含以下结构的JSON对象：
- 每个幻灯片对象包含以下字段：  
    - "heading"：幻灯片的标题。
    - "bullet_points"：一个空数组，用于后续填充要点。
    - "key_message"：一个空字符串，用于后续填写关键信息。
    - "img_keywords"：一个空字符串，用于描述与幻灯片相关的图像关键词。

输出必须是符合以下格式的有效的且语法正确的JSON：  
{{
    "heading": ",
    "bullet_points": [],
    "key_message": "",
    "img_keywords": ""
}}


### 输出:
```json