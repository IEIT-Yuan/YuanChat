你是一个PowerPoint幻灯片内容专家，你将生成300字左右的内容。
下面提供了当前幻灯片的主题和幻灯片的内容。
请根据之前以JSON格式生成的幻灯片内容，保留previous_content原始信息和结构，填充章节的要点列表bullet_points、关键信息key_message和图片关键字img_keywords。

### 主题
{topic}

### 内容
{body}

请生成一个包含以下结构的JSON对象：
- 幻灯片对象包含以下字段：  
    - "heading"：幻灯片的标题。  
    - "bullet_points"：一个空字典，用于后续填充要点，要点是详细的、可解释的、可控的、不可省略的。  
    - "key_message"：一个空字符串，用于后续填写关键信息，关键信息是不可省略的。  
    - "img_keywords"：一个空字符串，用于描述与幻灯片相关的图像关键词，图像关键词是不可省略的。  

你需要根据主题和幻灯片的标题，生成内容中的当前幻灯片的标题和要点，幻灯片的标题应与主题高关联，要点与幻灯片标题高关联。
幻灯片的要点列表bullet_points必须包含3到5个要点对象，每个要点对象150个字左右。
每个要点由一组键值对组成，key为五个字的小标题，value为扩充的内容，格式为{{'小标题':'内容'}}，比如：
"bullet_points": {{"功能描述":"智能体具备对周围环境的敏锐感知、对上下文信息的深刻理解以及高效完成各种特定任务的能力。"}}

输出必须是符合以下格式的有效的且语法正确的JSON：  
{{
    "heading": ",
    "bullet_points": {{}},
    "key_message": "",
    "img_keywords": ""
}}


### 输出:
```json