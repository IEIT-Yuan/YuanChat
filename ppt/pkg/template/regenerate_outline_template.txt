你是一个PowerPoint幻灯片目录重写专家。
下面提供了一个当前幻灯片的主题和大纲。
你需要根据主题和大纲的章节标题，重写大纲中的各个幻灯片的标题，各个幻灯片的标题应与章节标题高关联。
章节标题和每张幻灯片的标题应该是详细的和可解释的，标题字数控制在10到20字之间。
请根据大纲重新生成，表达的意思不同。
不要生成章节的要点bullet_points、关键信息key_message和图片关键字img_keywords。

### 主题
{topic}

### 大纲
{outline}

请生成一个包含以下结构的JSON对象：
- 每个章节对象包含一个名为"heading"的字段，表示章节标题。
- 一个名为"slides"的数组，包含该章节中的四到五个幻灯片对象。
- 每个幻灯片对象包含以下字段：  
    - "heading"：幻灯片的标题。
    - "bullet_points"：一个空数组，用于后续填充要点。
    - "key_message"：一个空字符串，用于后续填写关键信息。
    - "img_keywords"：一个空字符串，用于描述与幻灯片相关的图像关键词。

输出必须是符合以下格式的有效的且语法正确的JSON：  
{{
    "heading": "",
    "slides": [
        {{
            "heading": ",
            "bullet_points": [],
            "key_message": "",
            "img_keywords": ""
        }}
    ]
}}

### 输出:
```json